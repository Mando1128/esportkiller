{
  "name": "pulse-esports-live-server",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build:cloudflare": "node build-cloudflare.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@clerk/backend": "^3.17.1",
    "ws": "^8.18.0"
  }
}
